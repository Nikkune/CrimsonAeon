import React from 'react';
import {Stack} from "@mui/material";

const CheckEmail = () => {
    return (
        <Stack component="main" sx={{width: "calc(100% - 1rem)", height: "calc(100vh - 1rem)", background: "transparent", zIndex: 1000, position: "absolute", left: 0, top: "0", padding: 0.5 + "rem", overflowY: "auto", transition: "all ease 500ms"}}>

        </Stack>
    );
};

export default CheckEmail;
